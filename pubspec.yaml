name: layers
description: A new Flutter package which helps developer to show layers in the applications.
version: 0.0.1
homepage: "https://github.com/rajkumar07793/layers"

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: